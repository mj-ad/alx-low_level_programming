#include "dog.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *free_dog: a function that frees dogs
 *@d: details of dogs to be freed
 *
 *Return: void
 */

void free_dog(dog_t *d)
{
if (d != NULL)
{
free(d->name);
free(d->age);
free(d->owner);
}
}
