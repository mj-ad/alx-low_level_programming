#include <stdio.h>
#include <string.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (Perfect)
 */

int main(void)
{
char str[70];

strcpy(str, "\"Programming is like building a multilingual puzzle");

puts(str);

return (0); 
}
